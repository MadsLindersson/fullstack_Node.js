//We can't import json like we could with common js
//We would need to read the file as String
//And then parse it
// ES Module
// EcmaScript ES6

//export const cantImportJSONFiles = true;

export default 1234;